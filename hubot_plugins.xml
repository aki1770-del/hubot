<library path="hubot_costmap_filters">
  <class type="hubot::ZoneParameterFilter" base_class_type="nav2_costmap_2d::Layer">
    <description>
      Applies configured ROS parameter overrides to target nodes based on the mask
      value at the robot's pose. When a push is rejected, unanswered, or names an
      undeclared state, it latches the fault, keeps the costmap running, and
      publishes a zone_decision diagnostic saying the zone is not being enforced.
    </description>
  </class>
</library>
